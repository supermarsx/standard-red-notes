{
  "name": "@standard-red-notes/e2e",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=26.0.0"
  },
  "description": "Browser smoke tests for the Standard Red Notes web app (does it actually open?).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ops-load": "playwright test ops-load.spec.ts --project=chromium",
    "install-browser": "playwright install chromium",
    "screenshot:readme": "node ../scripts/capture-readme-screenshot.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
