node_modules/
.DS_Store
dist/
npm-debug.log
test/data/tmp/

/app/dist
.vscode
.idea
.env

data/*

# Bundled Hunspell spellcheck dictionaries are downloaded at build time via
# `yarn dictionaries:download` (scripts/download-dictionaries.mjs) and must not
# be committed. The directory itself is kept via app/dictionaries/.gitkeep.
app/dictionaries/*
!app/dictionaries/.gitkeep

codeqldb
yarn-error.log
