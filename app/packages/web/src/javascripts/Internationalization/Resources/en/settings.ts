/**
 * English strings for preference-pane bodies (General, Security, Appearance,
 * Backups, Sync, Plugins, Labs, etc. — the content inside each pane, distinct
 * from the `preferences` namespace which holds the pane titles/nav). Source of
 * truth: other locales fall back to these until translated.
 */
const settings = {}

export default settings
