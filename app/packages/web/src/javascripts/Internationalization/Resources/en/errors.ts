/**
 * English strings for error and status messages (toasts, inline validation,
 * failure notices, retry prompts). Source of truth: other locales fall back to
 * these until translated.
 */
const errors = {}

export default errors
