/**
 * English strings for generic dialogs (confirm/alert/prompt modals, toasts,
 * generic call-to-action copy reused across modals). Source of truth: other
 * locales fall back to these until translated.
 */
const dialogs = {}

export default dialogs
